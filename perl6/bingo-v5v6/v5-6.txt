#!/usr/bin/env perl						  #!/usr/bin/env perl6
								  
use strict;							  my $game = 0;
use warnings;							  my %draws;
use Data::Dumper;						  
$Data::Dumper::Sortkeys = 1;					  for (5,27,46,55,67), (14,23,32,47,62), (39,45,44,42,35), (69,49,1
								    my %h;
my $game = 0;							    %h{$_} = $_;
my $draws = {};							    $game++;
for ([5,27,46,55,67], [14,23,32,47,62], [39,45,44,42,35], [69,49,   %draws{$game} = %h;
  my $h;							  }
  @$h{@$_} = @$_;						  
  $game++;							  my %winners;
  $draws->{$game} = $h;						  
}								  ### Parse Block
								  {
my $winners;							   my @lines = 'bingo.txt'.IO.lines;
								   my @t = ();
### Parse Block							   my ($player, $board);
{								  
 my @lines;							   my $index = 0;
 { open (my $fh, '<', "bingo.txt") or die; local ($/) = undef; @l  my $line_count = @lines.elems;
 my $t = [];							  
 my ($player, $board);						   while ($index < $line_count) {
								    my $line = @lines[$index++];
 my $index = 0;							    next if ($line ~~ /^\s*$/);
 my $lines_count = @lines;					    $line = $line.trim;
 while ($index < $lines_count) {				    if ($line ~~ /Player/) {
  my $line = $lines[$index++];					     $player = $line.split(':')[1];
  next if ($line =~ /^\s*$/);					     my $tmp = @lines[$index++];
  chomp($line);							     if ($tmp ~~ /Board/) {
  if ($line =~ /Player:\s*(.*)\s*$/) {				      $board = $tmp.split(':')[1];
   $player = $1;						     }
   my $tmp = $lines[$index++];					     my $id = "{$board}:{$player}";
   if ($tmp =~ /Board:\s*(.*)\s*$/) {				  
    $board = $1;						     # Need to examine 5 horizontal, 5 vertical, two diagonals (for
   }								     my %in = (
   my $id = "$board:$player";					      "h1" => @t[0],
								      "h2" => @t[1],
   # Examine 5 horizontal, 5 vertical, two diagonals (forward and     "h3" => @t[2][0,1,3,4],
   my %in = (							      "h4" => @t[3],
    "h1" => $t->[0],						      "h5" => @t[4],
    "h2" => $t->[1],						  
    "h3" => [@{$t->[2]}[0,1,3,4]],				      "v1" => [ @t[0][0],@t[1][0],@t[2][0],@t[3][0],@t[4][0] ],
    "h4" => $t->[3],						      "v2" => [ @t[0][1],@t[1][1],@t[2][1],@t[3][1],@t[4][1] ],
    "h5" => $t->[4],						      "v3" => [ @t[0][2],@t[1][2],@t[3][2],@t[4][2] ],
								      "v4" => [ @t[0][3],@t[1][3],@t[2][2],@t[3][3],@t[4][3] ],
    "v1" => [ $t->[0][0],$t->[1][0],$t->[2][0],$t->[3][0],$t->[4]     "v5" => [ @t[0][4],@t[1][4],@t[2][3],@t[3][4],@t[4][4] ],
    "v2" => [ $t->[0][1],$t->[1][1],$t->[2][1],$t->[3][1],$t->[4] 
    "v3" => [ $t->[0][2],$t->[1][2],$t->[3][2],$t->[4][2] ],	      "df" => [ @t[0][0],@t[1][1],@t[3][3],@t[4][4] ],
    "v4" => [ $t->[0][3],$t->[1][3],$t->[2][2],$t->[3][3],$t->[4]     "db" => [ @t[0][4],@t[1][3],@t[3][1],@t[4][0] ],
    "v5" => [ $t->[0][4],$t->[1][4],$t->[2][3],$t->[3][4],$t->[4]    );
								  
    "df" => [ $t->[0][0],$t->[1][1],$t->[3][3],$t->[4][4] ],	     # Keep track of all numbers on a bingo card, helps in eliminat
    "db" => [ $t->[0][4],$t->[1][3],$t->[3][1],$t->[4][0] ],	     my @all = (|@t[0], |@t[1], |@t[2][0,1,3,4], |@t[3], |@t[4]);
   );								  
								     for %draws.keys -> $g {
   # Keep track of all numbers on a bingo card, helps in eliminat      # IF bingo card has less than 4 numbers from the drawing, no
   my @all = (@{$t->[0]}, @{$t->[1]}, @{$t->[2]}[0,1,3,4], @{$t->      if %draws{$g}{@all}.grep({$_.defined}).elems >= 4 {
								  	for %in.kv -> $k, $v {
   for my $g (keys %$draws) {					  	 %winners{$g}.push("{$id}:{$k}") if (%in{$k}.elems == %draw
    my $draw = $draws->{$g};					  	}
    # IF bingo card has less than 4 numbers from the drawing, no       }
    if (scalar(grep { defined } @{$draws->{$g}}{@all}) >= 4) {	      }
     for my $k (keys %in) {					     @t = ();
      push(@{$winners->{$g}}, "${id}:${k}") if (@{$in{$k}} == gre   } else {
     }								     push(@t, [ $line.split(' ') ]);
    }								    }
   }								   }
								  }
   $t = [];							  
  } else {							  %winners.say;
   push(@$t, [ split(' ', $line) ]);				  
  }								  
 }								  
}								  
								  
print Data::Dumper->Dump([$winners], ['Winners']);		  
